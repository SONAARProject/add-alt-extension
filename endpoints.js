/**
 * Change the endpoints to your local installation
 * These endpoints must use HTTPS
 */
const searchEndpoint = "https://<change-me>/clarifai/search";
const insertEndpoint = "https://<change-me>/clarifai/insertBuffer";